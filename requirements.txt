# The core parser/comparator uses only the Python standard library.
fastapi>=0.115,<1.0
# Base Uvicorn avoids native optional extras; install `uvicorn[standard]` later
# if file watching and higher-performance HTTP parsers are desired.
uvicorn>=0.30,<1.0
pytest>=8.0,<9.0
